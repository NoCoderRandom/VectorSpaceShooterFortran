# Assets

The current slice is code-generated: wire models, HUD text, starfield, audio tones, and particles are generated at runtime. This directory is reserved for future optional model definitions, curated wave scripts, or capture presets.
